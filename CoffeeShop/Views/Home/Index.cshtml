@model HomeViewModel

<h1>Отобранный кофе</h1>
<div class="row mt-3 mb-2">
    @{
        foreach (Coffee coffee in Model.FavCoffee)
        {
            @Html.Partial("AllCoffee", coffee)
        }
    }
</div>